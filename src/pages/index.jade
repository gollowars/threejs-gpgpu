block pageTitleBlock
  - pageTitle = "ページタイトル"

extends ./layout/_layout.jade

block content
  .movie-area#movieArea
  .grid-container
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text
    .grid-item
      .inner
        p.title Grid text

block bottomjs 
  script(src="#{path}js/main.js")
